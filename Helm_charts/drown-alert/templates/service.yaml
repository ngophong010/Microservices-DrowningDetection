apiVersion: v1
kind: Service
metadata:
  name: {{ include "drown-alert.fullname" . }}
  labels:
    {{- include "drown-alert.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "drown-alert.selectorLabels" . | nindent 4 }}
